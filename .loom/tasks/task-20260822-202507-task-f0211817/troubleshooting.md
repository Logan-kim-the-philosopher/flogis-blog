# 문제 해결 기록

- 브라우저 제어 스킬을 초기화했지만 연결 가능한 브라우저 목록이 비어 있어 자동 화면 캡처를 진행할 수 없었다. 개발 서버 HTML, 정적 빌드 HTML, 생성 CSS를 교차 확인했다.
- 최초 로컬 응답 검증 루프에서 zsh의 특수 배열명 `path`를 반복 변수로 사용해 해당 명령 내부의 `PATH`가 비워졌다. 변수명을 `archive_route`로 바꿔 재실행했고 세 경로 모두 정상 확인했다. 프로젝트 파일과 실행 중인 서버에는 영향이 없었다.
