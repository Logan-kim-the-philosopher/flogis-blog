# 문제 해결 기록

- 구현과 검증 과정에서 연결 실패는 발생하지 않았다.
- 인증 API는 `HTTP 200`을 반환했고 Astro dev 및 정적 빌드 모두 성공했다.
- 화면에 콘텐츠 카드가 없는 것은 연결 문제가 아니라 `study`, `meeting`, `work` 문서 수가 모두 0개이기 때문이다.
- 사용자가 대화에 토큰을 직접 제공했으므로 장기 사용 전 토큰 교체를 권장한다.
