# 결정

- 앞선 flex 레이아웃 변경은 방향 자체는 맞았으므로 유지했다.
- 문제는 Tailwind 유틸리티가 아니라 그 뒤에 선언된 전역 `html, body` 규칙의 cascade 충돌로 확정했다.
- `body` 높이를 별도 전역 값으로 다시 지정하지 않고 `SiteLayout`의 `min-h-screen`을 단일 높이 기준으로 사용했다.
- 페이지별 하단 패딩과 카드 크기는 이미지에서 문제 영역의 원인이 아니므로 변경하지 않았다.
