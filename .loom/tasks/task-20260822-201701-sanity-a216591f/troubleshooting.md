# 문제 해결 기록

- 생성 전 조회에서 기존 `sanity.imageAsset` 문서가 0개임을 확인했다.
- 스키마 필수 이미지 참조를 생략하지 않고 `public/favicon.ico`를 `image/png` 자산으로 업로드해 해결했다.
- 테스트 문서 생성, 참조 해석, dev 렌더링, 정적 빌드 과정에서 오류는 발생하지 않았다.
- 테스트 콘텐츠가 실제 `production` dataset에 남아 있으므로 검증 후 필요 시 문서와 이미지 자산 순서로 정리해야 한다.
