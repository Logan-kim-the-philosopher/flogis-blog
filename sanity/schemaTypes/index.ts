import { workType } from '../schemas/workType';
import { studyType } from '../schemas/studyType';
import { meetingType } from '../schemas/meetingType';
import { personType } from '../schemas/personType';
import { siteSettingsType } from '../schemas/siteSettingsType';

export const schemaTypes = [siteSettingsType, personType, studyType, workType, meetingType];
